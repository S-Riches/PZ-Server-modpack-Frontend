const SearchBox = () => {
  return (
    <div className="searchBox">
      <input type="text" name="searchBox" id="searchBox" />
      <button>Go</button>
    </div>
  );
};

export default SearchBox;
